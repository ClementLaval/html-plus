describe('SetObservedAttributes', () => {
  it('Should return none property', () => {
    // Given a new instance without @property() or observedAttributes
    // When is instancied
    // Then observedAttributes should be empty
  });
});